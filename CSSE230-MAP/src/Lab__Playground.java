public class Lab__Playground {

	@SuppressWarnings("deprecation")
	public static void main(String[] args) {
		/*
		 * we can use this for location information.
		 */
		// Browser browser = new Browser();
		// BrowserView view = new BrowserView(browser);
		//
		// JFrame frame = new JFrame("JxBrowser Google Maps");
		// frame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
		// frame.add(view, BorderLayout.CENTER);
		// frame.setSize(700, 500);
		// frame.setLocationRelativeTo(null);
		// frame.setVisible(true);
		//
		// browser.loadURL("http://www.rose-hulman.edu");

		// Date date = new Date();
		// System.out.println(date);

		/*
		 * we can use this for time.
		 */

		// while (true) {
		// TimeZone.setDefault(TimeZone.getTimeZone("Asia/Tokyo"));
		// SimpleDateFormat date_format = new SimpleDateFormat("dd-MM-yyyy EEEE
		// hh:mm:ss");
		// Date date = new Date();
		// String current_data_time = date_format.format(date);
		// System.out.println(current_data_time);
		// }

	}

	// public class ActionListene extends ActionListener {
	//
	// }

}
